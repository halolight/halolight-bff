# Server Configuration
PORT=3002
HOST=0.0.0.0
NODE_ENV=development

# CORS Configuration
# Use comma-separated values for multiple origins
# Example: http://localhost:3000,http://localhost:3001
CORS_ORIGIN=*

# JWT Configuration
# IMPORTANT: Change this in production! Use at least 32 characters
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRES_IN=7d

# Logging
# Levels: trace, debug, info, warn, error, fatal
LOG_LEVEL=info

# Backend Services (optional - for API aggregation)
# Configure the backend services you want to connect to
# The BFF will use the first available service based on priority

# Python API (FastAPI)
# HALOLIGHT_API_PYTHON_URL=http://localhost:8000

# Bun API (Hono)
# HALOLIGHT_API_BUN_URL=http://localhost:3000

# Java API (Spring Boot)
# HALOLIGHT_API_JAVA_URL=http://localhost:8080

# NestJS API
# HALOLIGHT_API_NESTJS_URL=http://localhost:3001

# Node.js API (Express)
# HALOLIGHT_API_NODE_URL=http://localhost:3003

# Go API (Gin)
# HALOLIGHT_API_GO_URL=http://localhost:8081

# Database (for future use with Prisma)
# DATABASE_URL=postgresql://user:password@localhost:5432/halolight

# Redis (for future caching)
# REDIS_URL=redis://localhost:6379
